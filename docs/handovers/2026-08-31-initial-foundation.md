# Handover: Initial Foundation

The repository now has a shared operating agreement, ignored local plugin checkout, core project documentation, and the first offline research vertical slice. The next machine should update `main`, install Python 3.11 plus dependencies, run the commands in `PROJECT_STATE.md`, and then begin the SEC EDGAR adapter slice.
